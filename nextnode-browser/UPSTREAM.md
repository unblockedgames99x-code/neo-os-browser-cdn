# NextNode browser runtime

Synced from https://nextnode9124.b-cdn.net/ on 2026-09-13T04:29:11.977Z.

This is the complete browser/proxy runtime used by NEO Browser. The embedding patch only makes its asset base relative and selects the upstream WISP endpoint without reading the cross-origin parent window.

Run `node scripts/sync-nextnode-browser.cjs` to refresh the pinned files and hashes.
